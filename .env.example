# Contact form endpoint (Cloudflare Worker / Resend relay)
# E.g., VITE_CONTACT_ENDPOINT=https://orivenza-mail.<your-subdomain>.workers.dev
VITE_CONTACT_ENDPOINT=

# Contact mailbox shown in UI fallback links
VITE_CONTACT_EMAIL=get.info@orivenza.com

# GST one-file demo endpoint (Cloudflare Worker)
# E.g., VITE_GST_DEMO_ENDPOINT=https://gst-ai-orivenza-api.<account>.workers.dev
VITE_GST_DEMO_ENDPOINT=

# Optional private compliance endpoint used by GST AI Chat
# If set in production, requests go directly to this URL.
# In local dev, Vite proxies it through /api/gst-compliance.
# If you only have one Worker for now, you can reuse the demo endpoint here.
VITE_GST_AI_COMPLIANCE_ENDPOINT=

# Remote GST application UI to embed behind the local access gate
VITE_GST_APP_URL=https://gst.orivenza.com/st-ai-compliance

# Base path for subpath hosting (only if not using a custom domain)
# E.g., VITE_BASE=/orivenza-site/ for GitHub Pages project site
# Leave blank or set to / for root domain or custom domain
VITE_BASE=/
